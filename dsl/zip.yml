ref: pack_zip
version: 1.0.1
inputParameters:
  - name: zip压缩解压命令
    ref: cmd
    type: STRING
    required: true
    description: zip压缩解压命令
spec:
  image: jianmurunner/pack_zip:1.0.0
  entrypoint:
    - /bin/sh
    - "-c"
  cmd:
    - ${JIANMU_CMD}